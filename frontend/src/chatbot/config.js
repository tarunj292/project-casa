const config = {
  initialMessages: [{ type: "text", id: 1, message: "Hi! How can I help you today?" }],
  botName: "SupportBot",
  customStyles: {
    botMessageBox: {
      backgroundColor: "#3B82F6",
    },
    chatButton: {
      backgroundColor: "#3B82F6",
    },
  },
};

export default config;
